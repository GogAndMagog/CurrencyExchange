package org.fizz_buzz.model.entity;

public record CurrencyEntity(int id,
                             String code,
                             String fullName,
                             String sign) {
}
